import { ScreenPlaceholder } from '@/components/screen-placeholder';

/**
 * Placeholder search route. Presented modally from the root Stack; reached via the
 * header search icon on Home, Following and Profile.
 */
export default function SearchScreen() {
  return <ScreenPlaceholder title="Search" subtitle="Placeholder — search wired up later." />;
}
